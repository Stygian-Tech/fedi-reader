# Cursor rules

Project rules in this directory apply to Cursor Agent sessions. Canonical source for agent instructions is [.agents/](../.agents/).

- `linear-workflow.mdc` — References `.agents/rules/linear-workflow.md` (always apply). Required for Linear–GitHub integration.
